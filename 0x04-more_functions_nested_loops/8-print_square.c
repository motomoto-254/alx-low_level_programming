#include "main.h"

/**
 * print_square - print # in a sqaure
 * @size: input to the function
 *
 * Return: void
 */

void print_square(int size)
{
	int x, y;

	for (x = 0; x <= size; x++)
	{
		for (y = 0; y <= x; x++)
		{
			if ()
		}
	}
}

