#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - program that prints its name
 * @argc: count to input arguments
 * @argv: pointer to string input
 *
 * Return: int
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	int i;

	i  = 0;
	printf("%s\n", argv[i]);
	return (0);
}
