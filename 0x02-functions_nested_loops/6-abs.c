#include "main.h"

/**
* _abs - computes the absolute value of an integer
* @j: input an int variable
* Return: integer after evaluating abs
*/

int _abs(int j)
{
	if (j < 0)
	{
		return (-j);
	}
	else
	{
		return (j);
	}
}

